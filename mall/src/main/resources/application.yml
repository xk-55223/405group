spring:
  profiles:
    active: db
  resources:
    static-locations: file:F:\spring


server:
  port: 80

pagehelper:
  helper-dialect: mysql

logging:
  file: spring.Log
  level:
    com:
      cskaoyan:
        mall:
          mallStart:
            mapper: debug
mybatis:
  type-handlers-package: com.cskaoyan.mall.mallStart.typehandler

